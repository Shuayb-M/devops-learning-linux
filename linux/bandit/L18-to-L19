
## Bandit Level 18 → Level 19

**Challenge:** 

The password for the next level is stored in a file readme in the homedirectory. Unfortunately, someone has modified .bashrc to log you out when you log in with SSH.

Commands you may need to solve this level
ssh, ls, cat


**Solution & Explanation:****
```

STEP 1: (Understand the problem)
# bandit18’s shell is configured to immediately exit on login
# this prevents interactive access, but NOT remote command execution

STEP 2: use ssh to run a single command remotely
ssh bandit18@localhost -p 2220 "cat readme"
# ssh can execute a command without starting an interactive shell
# the command runs before logout logic takes effect
# cat readme prints the password directly

STEP 3: copy the printed password
# this is the password for bandit19


```
**Password:** [cGWpMaKXVwDUNgPAVJbWYuGHVn9zl3j8]



**What I learned:** 

SSH does not require an interactive shell to run commands.

ssh user@host "command" → runs the command remotely and exits

shell logout tricks only block interactive sessions, not command execution

Even if the door slams shut, you can still slide a note under it if the system allows remote commands.
